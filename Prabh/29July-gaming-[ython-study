1. List Comprehension
We need to filter out bot accounts. Extract a list of all usernames from `players_df`. Then, use a **List Comprehension** to create a new list `real_players` that only contains usernames that do NOT start with 'bot_'. Print the results
# You can use players_df here
username_list = players_df['username'].tolist()

# Write your code here:
real_players=[x for x in username_list if not x.startswith('bot')]
Real_players
2. Pandas Basics
Filter `sessions_df` to find all hardcore sessions where `duration_mins` is strictly greater than 100. Print the resulting dataframe.
# Write your code here:
Print(sessions_df[sessions_df['duration_mins']>100])
3. Pandas Grouping
Calculate the total `duration_mins` played by each `player_id`. Print the aggregated result.
# Write your code here:
player_duration=sessions_df.groupby('player_id').agg(total_duration=('duration_mins','sum')).reset_index()
Player_duration
4. Pandas Joining
Merge `sessions_df` with `players_df` on the `player_id` column using an inner join to see which region played each session. Print the first few rows using `.head()`.
# Write your code here:
merged=pd.merge(sessions_df,players_df,on='player_id')
Merged.head()
5. Function Apply
Write a Python function `calculate_bonus(duration)` that returns 'High' if duration is > 60, and 'Low' otherwise. Apply this to the `duration_mins` column to create a new column `engagement_tier`. Print the dataframe.
# Write your code here:
def calculate_bonus(duration):
    if duration>60:
        return 'High'
    else:
        return 'Low'
sessions_df['engagement_tier']=sessions_df['duration_mins'].apply(calculate_bonus)
sessions_df[['engagement_tier','duration_mins']]
6. Data Cleaning
Some sessions crashed, resulting in missing `xp_gained` values (NaN). Fill these missing values with `0` in-place. Print the dataframe to verify.
# Write your code here:
sessions_df['xp_gained']=sessions_df['xp_gained'].fillna(0)
print(sessions_df)
7. String Manipulation
Make the `username` column in `players_df` uppercase for better display in the UI. Modify the column and print `players_df`.
# Write your code here:
print(players_df)
players_df['username']=players_df['username'].str.upper()
Print(players_df)
8. Time Series / Dates
Convert the `date` column in `sessions_df` into proper datetime objects. Then, extract the month into a new column called `month`. Print `sessions_df`.
# Write your code here:
sessions_df['date']=pd.to_datetime(sessions_df['date'])
sessions_df['month']=sessions_df['date'].dt.month

